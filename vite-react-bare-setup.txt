===============================
📜 VITE REACT CHEATSHEET - Spikebolt76
===============================

──────────────────────────────
1) STANDARD VITE + MANUAL CLEANUP
──────────────────────────────
COMMAND:
npm create vite@latest my-app -- --template react
cd my-app
npm install

MANUAL CLEANUP:
- Delete `App.css`
- Clear `App.jsx` to:
    export default function App() {
      return <h1>Hello World</h1>
    }
- Remove extra CSS imports in `main.jsx`
- Remove logo.svg

──────────────────────────────
2) CLONE / DE-GIT FROM YOUR GITHUB
──────────────────────────────
💡 Repo: https://github.com/Spikebolt76/vite-min-react

DEGIT COMMAND:
npx degit Spikebolt76/vite-min-react my-app
cd my-app
npm install
npm run dev

──────────────────────────────
(*) PERSONAL CLEAN TEMPLATE (Spikebolt76)
──────────────────────────────
📦 Folder Structure:
my-app/
  ├── src/
  │   ├── App.jsx
  │   ├── index.css
  │   └── main.jsx
  ├── package.json
  ├── vite.config.js
  ├── README.md
  └── .gitignore

📝 App.jsx:
export default function App() {
  return <h1>Spikebolt76 React Minimal</h1>
}

📝 main.jsx:
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)

📝 index.css:
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}
body {
  font-family: sans-serif;
}
---------------------
COMMANDS TO START:
npm install
npm run dev

──────────────────────────────
END OF FILE                  |
──────────────────────────────
